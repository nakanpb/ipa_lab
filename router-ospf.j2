$ mkdir templates
$ cd templates
$ vim router-ospf.j2
$ cat router-ospf.j2
router ospf {{ ospf_id }} vrf {{ vrf }}
 {% for net in network %}
 network {{ net.network }} {{ net.wc_mask }} area {{ area }}
 {% endfor %}
 {% if border == True %}
 default-infomaion originate
 {% endif %}